/** @type {import('next').NextConfig} */
const nextConfig = {
  // Mount-ready: Ben can reverse-proxy /wordpress to this app
  // or set basePath if serving under a subpath of another Next host.
};

export default nextConfig;
