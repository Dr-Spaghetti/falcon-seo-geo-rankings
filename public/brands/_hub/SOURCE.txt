Photoreal hub chrome — CLEAN composite (no SoT inpaint):
  meander-frame.png + column-left/right + seal-ring (gold braid only)
  + smooth empty dark field. ZERO baked type/logo/crest glyphs.
  Crest + justify + justify local mark render LIVE in ClientHub DOM.
Assets:
  hero-chrome-plate.png  — clean composite plate (empty field)
  meander-frame.png      — frame-only transparent interior
  column-left/right.png  — Ionic columns (no crest pixels)
  seal-ring.png          — gold braid only; interior fully transparent
  map-sat-thumb.png      — satellite thumb + teal pin
  watermark-ground.png   — tall soft-fade ground (no hard bottom edge)
  justify-mark.png       — header two green arrows
Do NOT inpaint SoT crops into the plate. Do NOT bake firm type/logo/DLC.
